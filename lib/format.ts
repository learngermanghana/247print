import { BUSINESS } from "@/lib/constants";

export const telHref = `tel:${BUSINESS.phoneLink}`;
export const mailTo = `mailto:${BUSINESS.email}`;

export const currentYear = new Date().getFullYear();
